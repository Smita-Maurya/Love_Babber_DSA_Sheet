bool searchPattern(string str, string pat)
{
    int n=pat.size();
    int m=str.size();
    if(m<n){
        return false;
    }
    for(int i=0; i<m-n+1 ; i++){
        if(str.substr(i , n)==pat){
            return true;
        }
        
    }
    return false;
  
}
